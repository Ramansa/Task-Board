{
  "name": "task-board-cloudflare",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev public --d1 DB=task_board --compatibility-date=2025-01-01",
    "deploy": "wrangler pages deploy public",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "wrangler": "^4.13.2"
  }
}
